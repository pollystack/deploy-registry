version: '3.8'

services:
  registry:
    image: registry:2
    ports:
      - "5555:5000"
    environment:
      REGISTRY_AUTH: htpasswd
      REGISTRY_AUTH_HTPASSWD_REALM: "Registry Realm"
      REGISTRY_AUTH_HTPASSWD_PATH: /auth/htpasswd
      REGISTRY_HTTP_TLS_CERTIFICATE: /certs/domain1.lan/fullchain.pem
      REGISTRY_HTTP_TLS_KEY: /certs/domain1.lan/privkey.pem
      REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /var/lib/registry
      # Updated CORS configuration
      REGISTRY_HTTP_HEADERS_Access-Control-Allow-Origin: '["http://domain1.lan:8080"]'
      REGISTRY_HTTP_HEADERS_Access-Control-Allow-Methods: '["HEAD", "GET", "OPTIONS", "DELETE"]'
      REGISTRY_HTTP_HEADERS_Access-Control-Allow-Headers: '["Authorization", "Accept", "Cache-Control"]'
      REGISTRY_HTTP_HEADERS_Access-Control-Expose-Headers: '["Docker-Content-Digest"]'
      REGISTRY_HTTP_HEADERS_Access-Control-Allow-Credentials: '[true]'
    volumes:
      - ./auth:/auth
      - ./certs:/certs
      - ./data:/var/lib/registry
    restart: always

  registry-ui:
    image: joxit/docker-registry-ui:latest
    ports:
      - "8080:80"
    environment:
      - REGISTRY_TITLE=Private Docker Registry
      - REGISTRY_URL=https://domain1.lan:5555
      - SINGLE_REGISTRY=true
      - REGISTRY_ALLOW_BASIC_AUTH=true
      - REGISTRY_USER=admin    # Use the username you created
      - REGISTRY_PASS=admin    # Use the password you created
    depends_on:
      - registry
    restart: always